/**
 * Testing Library custom matchers for DOM assertions.
 *
 * Examples:
 * expect(element).toBeInTheDocument();
 * expect(element).toHaveTextContent(/text/i);
 * expect(button).toBeDisabled();
 */

import "@testing-library/jest-dom";
